[workspace]
members = [
    "cardano-chain-sync",
    "cardano-mempool-sync",
    "cardano-submit-api",
    "spectrum-offchain",
    "spectrum-offchain-cardano",
    "spectrum-cardano-lib",
    #"spectrum-offchain-amm",
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
lto = true