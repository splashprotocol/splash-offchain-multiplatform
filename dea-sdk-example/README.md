# DEA SDK Example

Replay a normalized fixture JSON through `dea-sdk` and print findings.

## Run

```bash
cargo run -p dea-sdk-example -- dea-sdk/tests/fixtures/front_run_basic.json
```

## Input

The input is a `ReplayInput` JSON document matching the `dea-sdk` public domain
schema.

